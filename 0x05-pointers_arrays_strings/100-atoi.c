#include "holberton.h"
/**
 *
 *
 *
 *
 *
 *
 *
 *
 */

int _atoi(char *s)
{
	int i;
	int a*;

	for (i = 0; s[i] != 0; i++)
	{
		if (s[i] >= '0' && s[i] <= '0')
		{
			a[i] = s[i]
		}
	}
	return (a);
}
