#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints number of command arguments
 *
 * @argc: number of command arguments
 *
 * @argv: Array of command arguments
 *
 * Return: 0
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);

	return (0);
}
