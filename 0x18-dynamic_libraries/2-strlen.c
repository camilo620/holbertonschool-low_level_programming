#include "holberton.h"
/**
 * _strlen - returns the size of a string
 *
 * Return: size of the string
 *
 * @s: string to be sized
 *
 */

int _strlen(char *s)
{
	int l;

	for (l = 0; s[l] != '\0'; l++)
	{
	}
	return (l);
}
